import styled from 'styled-components';

export const HeadingLabel = styled.h4`
  font-size: 1rem;
  font-weight: 600;
  color: var(--color-primary-text);
  margin: 0;
  padding: 0.5rem 0 0 0;
  align-self: end;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
